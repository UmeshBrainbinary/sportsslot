class GroundListModel {
  String? subGroundId;
  List? image;
  String? title;
  String? time;
  dynamic price;
  List? timeShifts;

  GroundListModel(this.subGroundId, this.image, this.title, this.time, this.price, this.timeShifts);
}
